{
  "name": "nexusflow-crud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build"
  },
  "devDependencies": {
    "@vercel/node": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
